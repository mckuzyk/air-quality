.pio
.clang_complete
.gcc-flags.json
.ccls

# clangd compile database — regenerated by `pio run -t compiledb`,
# contains absolute paths specific to this machine
compile_commands.json

# clangd's local index/cache, if it creates one
.cache/

# macOS
.DS_Store

# Secrets
src/secrets.h
include/secrets.h
